import {chromium} from '@playwright/test';const browser=await chromium.launch({channel:'msedge',headless:true,args:['--enable-unsafe-swiftshader']});const page=await browser.newPage();page.on('pageerror',e=>console.log('ERROR',e.stack));page.on('console',e=>{if(e.type()==='error')console.log('CONSOLE',e.text())});await page.goto('http://127.0.0.1:3001/about');await page.waitForTimeout(2000);console.log((await page.locator('body').innerText()).slice(0,600));await browser.close();
